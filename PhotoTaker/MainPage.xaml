<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:local="clr-namespace:PhotoTaker" 
             xmlns:custom="clr-namespace:PhotoTaker.Custom"
             Title="MainPage"
             x:Class="PhotoTaker.MainPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <custom:PhotoTakerView x:Name="photoTakerView" Grid.Row="0" Camera="Rear" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" />
        
        <Button Grid.Row="1" Text="Save" x:Name="saveButton" />
    </Grid>
</ContentPage>